package classworks.lesson22_20230605.junitExamples;

import static org.junit.Assert.*;

public class JUnitTestTest {

    /*

    assertEquals(Object expected, Object actual) - проверяет,
    равны ли передаваемые объекты

    assertTrue(boolean flag) - проверяет, возвращается ли переданное значение - true


    assertFalse(boolean flag) - проверяет, возвращается ли переданное значение - false


    assertNull(Object object) - проверяет, является ли объект null

    assertSame(Object firstObject, Object secondObject) - проверяет,
    ссылаются ли передаваемые значения на один и тот же объект

    assertThat(T t, Matcher<T> matcher) - проверяет, удовлетворяет ли t условию,
    указанному в Matcher

     */

}